# T17: 离线配对打磨 · 电脑端主次重排 + 断线快捷重配（offer 端）

- 状态：✅ 代码完成（桌面 offer 页粘贴为主/扫码降级/重新生成进角落 + 断线警告旁「重新配对」一步回本端 offer 页）
- 阻塞：无（T07/T13/T14 已完成，可立即开工）
- 被阻塞者：无
- 引用：SPEC §5.3（两跳体验打磨条目「电脑端主次重排」「断线快捷重配」）；ADR-0007；T14（设备分工——电脑默认免摄像头，主路径粘贴）；T13（断线重新配对提示现状）
- 来源：2026-08-15 访谈（Q4 选定条目 b + e；完全离线是主场景，断线重连概率不低）
- 完成备注：`npm test` 335/335（pairGuide 新增 rePairAction 5 测 + 打磨文案测试）；build + lint ✓；e2e 24/24（新增 T17 两步：offerer 断线重配 → 回 offer 页重新生成码；answerer 断线重配 → 保持接收角色回扫码相位）

## 目标

离线配对第一跳（offer 端）与断线重连的体验打磨，两个独立改进：

1. **电脑端主次重排**：offer-show 页按钮平级现状（重新生成 / 扫码对方的回码 / 复制配对码 + 粘贴框）改为——「粘贴回码」为唯一主操作（已常驻），「扫码对方的回码」降为次要入口，「重新生成」收进角落；电脑默认免摄像头（T14），主路径就是粘贴，按钮/文案不喧宾夺主
2. **断线快捷重配**：断线警告（连接已断开）旁直接提供「重新配对」按钮，点击一步回到本端 offer 页（保持本端角色，不重走 pick 页）；重新配对后自动从 bitfield 断点续传（复用现有 resume 流程）

## 验收标准（done when）

- [x] 电脑端 offer-show 页：「粘贴回码」框为唯一主操作（常驻、accent 高亮视觉突出）；「扫码对方的回码」降为次要入口（details 折叠，含可用摄像头取景）；「重新生成」收进二维码右上角落（小字、低透明）；文案不喧宾夺主
- [x] 手机端 offer-show 保持现状主操作（扫码对方的回码在按钮区），不受电脑端重排影响（手机↔手机场景仍以扫码为便）
- [x] 断线警告（连接已断开：重新配对后自动续传）旁新增「重新配对」按钮：一步回本端 offer 页（保持本端角色，不重走 pick——offerer 重新出码 / answerer 保持接收角色回扫码相位，rePairAction 纯函数编码），自动重新生成配对码；配对成功自动收起的面板在收起态也可见警告 + 按钮（everOpened 门控，不打扰纯在线用户）
- [x] 重新配对完成后自动从 bitfield 断点续传（复用 T06/T07 现有 resume_manifest 流程，不重传已收数据）
- [x] 按钮标签/文案变更集中到 pairGuide 系纯逻辑模块（PairPolishLabels + rePairAction）并单测（+9 测）；`npm test` 全量绿（335/335）；e2e 随文案更新（24/24）；`npm run build` + lint 通过；code-review 双轴通过（评审意见全部落实：answerer 角色保持、收起态入口、粘贴视觉突出、标签集中补全、ScannerVideo 去重）

## 备注

- 电脑端主路径仍是「粘贴回码」（T14）；「扫码对方的回码」是手动切换项，保留但降级
- 断线快捷重配只改变入口位置与相位跳转，不改变重新配对/续传协议（SPEC §3.4/§5.3）
- 与 T16 同在 OfflinePair.tsx，建议在其后串行实现
