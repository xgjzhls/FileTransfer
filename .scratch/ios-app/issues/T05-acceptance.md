# T05: 收尾与真机验收

**状态:** ⏳ 文档同步完成（2026-08-16）；真机验收项待用户执行（见下）

**完成记录（文档）:**
- README.md（新建）：app 构建/部署/7 天重签说明、壳内与网页版差异（数据不迁移）
- app 内数据不迁移提示：设置页（IS_NATIVE 时显示「app 与网页版数据不互通」）
- SPEC §8 里程碑 9 更新为已完成；CONTEXT 部署现状 + 风险 + 拍板更新；ADR-0008 后果复核（Xcode 已装、SW 禁用已实现、真机验证标记）

**待用户执行的真机验收（iPhone 11）：**
1. 打开 app（已安装）→ 首页配对/收发正常（在线 PIN + 离线扫码）
2. Spike 测试 4：选文件夹跑插件探针（嵌套目录还原 + 吞吐 + abort）
3. 收 ≥1GB 大文件 → 「导出到文件夹…」全程不崩、内存有界（观察）、「文件」App 可见且目录树还原
4. 批量 >10 文件导出 + 取消中途 + 重名文件追加序号
5. 分享面板（次级）可用：单文件/批量

**阻塞:** T03、T04

**被阻塞者:** 无

**引用:** SPEC §8 里程碑9;ADR-0008 后果(OPFS 数据不迁移、7 天签名、SW 不可用)

## What to build

正式收尾:大文件导出内存上界与目录树还原的真机验收;网页版既有数据不迁移的提示;7 天签名说明;文档同步。交付「正式可用」状态。

## 方案

- 真机验收:≥1GB 单文件 + 批量(>10 文件)导出,验证峰值内存与目录树
- app 内对网页版已有数据给出「不随 app 迁移」提示(用户需重新接收)
- README/文档补充 7 天签名重签说明
- 文档同步:SPEC 里程碑勾选、CONTEXT 状态更新、ADR 后果复核

## 验收标准(done when)

- [ ] ≥1GB 单文件 + 批量导出真机验收通过(内存上界、目录树、速度达标)
- [ ] 数据不迁移提示可见;README 含 7 天重签说明
- [ ] 文档同步完成;`npm test`/build/lint 全绿
